profile:
  bootloader:
    name: grub2
    timeout: 1
  parameters:
    bootpartition: "false"
    firmware: uefi
    devicepersistency: by-label
    filesystem: xfs
    image: vmx
    kernelcmdline:
      console: ttyS0
      net.ifnames: 0
      dis_ucode_ldr: []
  size: 10240
